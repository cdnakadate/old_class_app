<%= form_for @eval, :html => { :class => "form-horizontal eval" } do |f| %>

    <% if @eval.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@eval.errors.count, "error") %> prohibited this eval from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @eval.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>



  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            evals_path, :class => 'btn btn-default' %>

<% end %>
